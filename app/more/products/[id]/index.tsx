import { useLocalSearchParams } from 'expo-router'; import { ProductDetail } from '@/components/detail-screens'; export default function(){const{id}=useLocalSearchParams<{id:string}>();return <ProductDetail id={id}/>}
