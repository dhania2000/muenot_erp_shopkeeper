import { useMemo,useState } from 'react'; import { FlatList,Pressable,StyleSheet,Text,View } from 'react-native'; import { Link } from 'expo-router'; import { colors } from '@/constants/theme'; import { CustomerCard } from '@/components/common'; import { EmptyState,Icon,Input } from '@/components/ui'; import { customers } from '@/features/demo/data'; import { Screen } from '@/components/screen';
export default function Customers(){const[q,setQ]=useState('');const data=useMemo(()=>customers.filter(x=>x.name.toLowerCase().includes(q.toLowerCase())||x.phone.includes(q)),[q]);return <Screen scroll={false}><View style={s.header}><Text style={s.title}>Customers</Text><Link href="/customers/new" asChild><Pressable style={s.add}><Icon name="person-add" color="#fff"/></Pressable></Link></View><View style={s.pad}><Input placeholder="Search by name or phone" value={q} onChangeText={setQ}/><Text style={s.count}>{data.length} customer{data.length===1?'':'s'}</Text></View><FlatList data={data} keyExtractor={x=>x.id} renderItem={({item})=><CustomerCard customer={item}/>} ListEmptyComponent={<EmptyState icon="people-outline" title="No customers found" description="Try a different search, or add a new customer."/>}/></Screen>};const s=StyleSheet.create({header:{flexDirection:'row',justifyContent:'space-between',alignItems:'center',padding:16},title:{fontSize:21,fontWeight:'800',color:colors.text},add:{width:40,height:40,borderRadius:20,backgroundColor:colors.primary,alignItems:'center',justifyContent:'center'},pad:{paddingHorizontal:16,gap:10},count:{fontSize:12,color:colors.muted}});
