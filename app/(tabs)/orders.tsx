import { useState } from 'react'; import { FlatList, Pressable, StyleSheet, Text, View } from 'react-native'; import { Link } from 'expo-router'; import { colors } from '@/constants/theme'; import { OrderCard } from '@/components/common'; import { EmptyState, Icon } from '@/components/ui'; import { orders } from '@/features/demo/data'; import { Screen } from '@/components/screen'; const filters=['all','new','processing','completed','cancelled'] as const;
export default function Orders(){const [filter,setFilter]=useState<typeof filters[number]>('all');const data=orders.filter(x=>filter==='all'||x.status===filter);return <Screen scroll={false}><View style={s.header}><Text style={s.title}>Orders</Text><Link href="/orders/new" asChild><Pressable style={s.add}><Icon name="add" color="#fff"/></Pressable></Link></View><View style={s.filters}>{filters.map(x=><Pressable key={x} onPress={()=>setFilter(x)} style={[s.filter,filter===x&&s.active]}><Text style={[s.filterText,filter===x&&s.activeText]}>{x[0].toUpperCase()+x.slice(1)}</Text></Pressable>)}</View><FlatList contentContainerStyle={s.list} data={data} keyExtractor={x=>x.id} renderItem={({item})=><OrderCard order={item}/>} ListEmptyComponent={<EmptyState icon="bag-outline" title="No orders here" description="Orders matching this filter will show up here."/>}/></Screen>};const s=StyleSheet.create({header:{flexDirection:'row',justifyContent:'space-between',alignItems:'center',padding:16},title:{fontSize:21,fontWeight:'800',color:colors.text},add:{width:40,height:40,borderRadius:20,backgroundColor:colors.primary,alignItems:'center',justifyContent:'center'},filters:{flexDirection:'row',gap:6,paddingHorizontal:16,paddingBottom:12},filter:{paddingVertical:8,paddingHorizontal:8,borderRadius:11,backgroundColor:'#edf1ef'},active:{backgroundColor:colors.primary},filterText:{fontSize:10,fontWeight:'800',color:colors.muted},activeText:{color:'#fff'},list:{paddingHorizontal:16,gap:9,paddingBottom:24}});
